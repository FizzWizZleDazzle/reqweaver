# Site configuration compiled into the static build. `npm run build:web`
# stages this file as public/data/siteconfig.json and the app reads it at
# startup.

# Goal encoding service (workers/encode). A student's free-text goal is
# posted here and comes back as a vector in the same space as the course
# vectors. Leave it empty until the endpoint is deployed: goals a school
# precompiled still steer plans, and any other wording is reported as not
# applied rather than failing.
encode_api: ''
